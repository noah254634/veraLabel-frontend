import React from "react";
import { Outlet } from "react-router-dom";
import { AdminLayout } from "./modules/admin/layout/AdminLayout";
export const App=()=>{
    return(
        <div>
            <h1 className="text-lg bg-black">Hello Noah khaemba,this is the first sentence for veraLabel.ai</h1>
        </div>
    )
}